import React from 'react';

const Home = () => {
  return (
    <div>
      <h1>Welcome to RAG Document App</h1>
      <p>Navigate to /login, /query, or /upload to use the app.</p>
    </div>
  );
};

export default Home;